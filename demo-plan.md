This is not a vibe coding because I have check all generated code,
vibe coding don't check the code, only finall result.

How I did autofixer:
- workflow:
    - spec 
    - generate
    - rollback 
    - fix spec
    - generate
    - rollback
    - fix spec
    - ... until you got good result

    - problems:
        - to undertand is good we have to read A LOOT OF CODE! again and again


- keet it simple as posible
    - edit spec
    - push `git diff` to LLM
    - check result


Spec is current state. If we need to iterate the progress use Issue.
Issue add new spec and start workflow.

Idea:
We work only with spec and issues. All code generated by ai agents.
!!! We need style guide for specs.
!!! Code review still required.

Notes:
- Don't care about token price, we have good budget now to test ideas.
- 15 min code generation - half day reading and understanding what we have
    - 15$/h ? 
    - human is a bottle neck

Show new cursor commands.

https://github.com/madebyaris/spec-kit-command-cursor/tree/main


### Exp. of using for spec generation:
gpt-5.3-codex - 0.03$ but bad result
composer-1.5 - 0.02$ bad also, but better then gpt-5.3-codex
claude-4.6-opus-high-thinking - 0.18$ good result

gpt-5.3-codex  - found code into git tree and restore it instead of create a new one

One more reason to stay on 2 steps before edge.
Generate result with diff LLM and compile the best mix version.

They all is working.